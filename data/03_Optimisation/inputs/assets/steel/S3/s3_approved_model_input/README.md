# S3 Approved Model Input

This directory is the approved-input shell for future `S3` accounting parameters.

`S3.0a` approves no numerical rows. No file in this directory may contain executable approved model inputs until a later human review and promotion step explicitly permits it.
